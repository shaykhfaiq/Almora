<div class="container py-5 text-center">
  <div class="alert alert-success p-5 rounded-4 shadow-sm border border-success">
    <h2 class="mb-3">🎉 Order Placed Successfully!</h2>
    <p class="lead">Thank you for shopping with <strong>Almora</strong>. Your order is confirmed and will be delivered shortly.</p>
    <a href="/" class="btn btn-outline-success mt-3 rounded-pill px-4 py-2 fw-semibold">
      ⬅ Back to Home
    </a>
  </div>
</div>

<script>
  // Clear local cart after successful order
  localStorage.removeItem("cart");

  // Reset cart count badge
  const badge = document.getElementById("cart-count");
  if (badge) badge.innerText = "0";
</script>
